const axi = axios.create();
const Toast = Swal.mixin({
// success
// error
// warning
// info
// question
  toast: true,
  position: 'top-end',
  showConfirmButton: false,
  timer: 1000
});

Vue.component('tree', {

})